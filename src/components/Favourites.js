import React from 'react';
import Sidebar from './Sidebar';
const Favourites = () => {
    return (
        <body>
        <header >
            
        <Sidebar/>

        </header>
        </body>
    );
};

export default Favourites;